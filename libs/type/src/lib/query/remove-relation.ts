/**
 * Options for removing a relation.
 *
 * This type is equivalent to `AddRelationOptions`, and can be used
 * wherever `AddRelationOptions` is used to specify the options for
 * removing a relation.
 *
 * @see AddRelationOptions
 */
import { AddRelationOptions } from './add-relation';

export type RemoveRelationOptions = AddRelationOptions;
