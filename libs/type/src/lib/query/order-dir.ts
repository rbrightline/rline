/**
 * Represents the direction of sorting in a query.
 *
 * @typedef {('ASC' | 'DESC')} OrderDir
 *
 * @example
 * // Usage example:
 * const order: OrderDir = 'ASC';
 */
export type OrderDir = 'ASC' | 'DESC';
