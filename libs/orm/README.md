# orm

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build orm` to build the library.

## Running unit tests

Run `nx test orm` to execute the unit tests via [Vitest](https://vitest.dev/).
